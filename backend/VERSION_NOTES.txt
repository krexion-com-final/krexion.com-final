# Force redeploy trigger 1782500000
# v2.1.80 — 2026-07-03 — Link-level Pro-Referrer (RUT-style, per-click)
#   Customer request addressed:
#     "reffer system rut job hain wo sab jab link banaai wahan bhi
#      use kar sakein taakay link banane ke baad customer link
#      kahin bhi paste kare aur referrer system perfect kaam kare
#      beshak wo rut job na chalaen — manual click par bhi kaam kare."
#
#   The full RUT referrer engine now runs at CLICK time on any tracking
#   link that opts in via the new `referrer_pro_enabled` toggle. Every
#   click on a saved link produces a fresh weighted-random platform
#   pick, brand-tagged UTM campaign, real click-ID (fbclid/gclid/ttclid/
#   msclkid/etc.), sec-fetch-* headers matching that platform, and
#   optionally a Referer-Wrapper bounce (l.facebook.com/l.php?u=...,
#   www.google.com/url?q=..., t.co/... etc.) so the offer network sees
#   a REAL platform domain as the incoming Referer header.
#
#   1) BACKEND — server.py
#      - LinkCreate / LinkUpdate / LinkResponse: 13 new pro-referrer
#        fields, all optional with backend-safe defaults. Existing
#        callers that omit them behave IDENTICALLY.
#      - create_link persists the new fields into the link doc.
#      - update_link needs no code change — its generic
#        model_dump(exclude_unset=True) loop already picks up any
#        new optional fields for partial-update calls.
#      - Click handler (`/r/{short_code}` redirect): the new
#        pro-referrer block runs AFTER the signed _kx_src handshake
#        (so RUT-driven visits still win) and BEFORE the classic
#        simulate_platform param generator. It calls
#        referrer_pro.resolve_pro_visit() with the link's saved
#        settings and feeds the chosen platform back into the
#        existing UTM/click-id generator — zero rewrite of the
#        proven click-decoration code.
#      - Wrapper-redirect is a separate opt-in field. When enabled,
#        the final 302 Location is rebuilt from the resolved
#        wrapper URL with the fully-decorated target embedded in
#        the wrapper's inner param, and Referrer-Policy is forced
#        to `unsafe-url` so the wrapper domain is the actual
#        Referer the offer receives.
#      - New endpoint: POST /api/links/preview-referrer — runs the
#        SAME resolve engine N (default 20) times against a mixed
#        UA pool and returns per-sample platform / referer /
#        utm_* / esp / click-id so the customer can eyeball the
#        traffic mix in the UI before publishing the link.
#
#   2) BACKEND — referrer_pro.py
#      - parse_weighted_pool now also accepts the natural
#        `facebook:50,instagram:30,google:20` colon-format in
#        addition to the existing JSON and equal-weight comma-list.
#        Purely additive — every existing caller (RUT jobs,
#        ReferrerStats, etc.) is untouched.
#
#   3) FRONTEND — LinksPage.js
#      - New collapsible "Advanced Referrer System (RUT-style)" card
#        under the classic "Referrer Simulation" section on the
#        link create/edit dialog. Hidden by default; auto-opens
#        when editing a link that already has it enabled.
#      - Master toggle + platform pool + brand + search-engine +
#        country + keywords + optional email_weights JSON + four
#        boolean toggles (social wrapper / inapp deep path /
#        strip search path / network click chain) + wrapper-redirect
#        opt-in. All fields default OFF/EMPTY so the toggle acts
#        as a hard on-off switch.
#      - "Preview 20 Sample Clicks" button opens a modal with a
#        platform-distribution mini-bar-chart + a per-sample table
#        (UA type, platform, referer, utm_source, utm_medium)
#        so customer sees exactly what mix their link will send.
#
#   Backward-compatibility (verified by testing_agent, 9/9 passed):
#     - Old links (docs without ANY pro-referrer keys) load cleanly
#       through LinkResponse defaults; click handler falls into the
#       untouched legacy simulate_platform branch — zero shift.
#     - Partial updates via PUT /api/links/{id} touch only the sent
#       fields; forced_source / referrer_mode / simulate_platform /
#       url_params / offer_url / name all stay intact.
#     - RUT-signed visits (`_kx_src` handshake) still take priority
#       over link-level pro-referrer — RUT jobs unaffected.
#
# v2.1.79 — 2026-07-03 — Native Local PC Dashboard "stuck on checking..." fix
#   Customer report addressed:
#     "native app run ki 2 hours ho gaye ye checking pe hi hai kuch
#      bhi chal ni raha." — screenshot showed Backend Engine, Local
#      Database, and krexion.com link all frozen at "checking..." state
#      with CPU 0% / RAM 0 GB, license blank.
#
#   Root cause: dashboard.js polled /api/desktop/stats every 2 s and
#   silently swallowed every failure — the initial "checking..." HTML
#   never got overwritten when the KrexionBackend NSSM service on the
#   customer's PC failed to come up. No timeout, no retry counter,
#   no diagnostic surface — the customer had no way to know what
#   was wrong.
#
#   1) DESKTOP — static/dashboard.js
#      - Per-fetch 4 s AbortController timeout so "connection
#        refused" and "endpoint hung" are distinguishable in the
#        surfaced error message instead of the browser's opaque
#        generic TypeError.
#      - _diag state tracks firstFailAt / consecutiveFailures /
#        lastError / everSucceeded / lastSuccessAt across polls.
#      - Progressive failure copy — no more perpetual "starting up":
#          8 s   -> yellow "not responding for 8s"
#         20 s   -> open Diagnose panel with actionable next steps
#         60 s   -> red "Backend offline for Nm Ns"
#      - Downstream cards (DB, Cloud) now flip to "waiting for
#        backend" / "last check failed" on failure so the UI
#        stops lying with a stale "checking...".
#      - Success resets all diagnostic state and hides the panel.
#      - Retry-Now button fires an immediate poll (no 2 s wait).
#
#   2) DESKTOP — static/index.html
#      - New <section id="diagnose-panel"> under the update banner:
#        header with icon + one-line reason, stat row
#        (downtime / failed checks / last error), an ordered
#        instructions list (Services.msc -> KrexionBackend ->
#        Restart, then also start KrexionDatabase, then check
#        the log file), and a copy-to-clipboard button for the
#        log file path.
#
#   3) DESKTOP — static/style.css
#      - .diagnose-panel + .diagnose-danger variants matching the
#        existing update-banner visual language, warn-orange border
#        under 60 s -> red-danger border past 60 s.
#

# Force redeploy trigger 1782418017
# v2.1.78 — 2026-07-02 — IPv6 support + VPS load-reduction + security hardening
#   Customer reports addressed:
#     A) "Clicks page pe kaafi rows pe IP show ni hota" — IPv6-only
#        mobile-carrier visitors (Jio / T-Mobile / Zong / Airtel etc.
#        via Cloudflare CF-Connecting-IP as an IPv6) were being dropped
#        by the strictly-IPv4 header parser. Every real visitor now has
#        a stored IP.
#     B) "VPS stuck ho jata hai jabki heavy jobs PC pe hain" — the
#        bridge cache pre-warmer was firing one 55-second bridge task
#        per online-heartbeat user every 25 s, saturating MongoDB with
#        polls. Now capped + UI-active-only.
#
#   1) IPv6 SUPPORT (backend/server.py)
#      • get_all_client_ips() rewritten — extracts both IPv4 and IPv6
#        from CF-Connecting-IP / X-Forwarded-For / X-Real-IP /
#        True-Client-IP / peer, normalises v6, keeps the infra-prefix
#        skiplist for false-dup prevention.
#      • click_doc now stores `ipv6` (was indexed but never written).
#      • primary_ip_for_storage falls back to IPv6 when no IPv4 —
#        Clicks table never shows `-` for a real visitor anymore.
#      • Duplicate detection is now IP-family-agnostic: same IPv6
#        repeat visits are blocked (previously allowed because IPv6
#        was explicitly excluded).
#      • ClickResponse Pydantic model exposes the new ipv6 field so
#        the frontend actually receives it.
#      • Block-page display + cookie-fallback IP now surface IPv6 so
#        users no longer see "IP: unknown" on IPv6-only blocks.
#      • Old legacy get_client_ip() reduced to a thin shim over
#        get_all_client_ips().
#
#   2) FRONTEND (ClicksPage.js)
#      • "IPv4" column shows ONLY IPv4 (was falling back to `ip_address`
#        which after the IPv6 fix contained IPv6 — confusing UX). IPv6
#        column continues to render v6.
#      • CSV export toast shows a `truncated` warning when the user
#        has more clicks than the export cap.
#
#   3) VPS LOAD REDUCTION (backend/server.py)
#      • Bridge cache pre-warmer: cycle 25 s → 30 s, max 10 concurrent
#        pre-warm tasks (env: BRIDGE_PREWARM_MAX_PARALLEL), and only
#        fires for users whose FRONTEND is actively polling
#        /api/bridge/me/local-status within the last 3 min (env:
#        BRIDGE_PREWARM_UI_WINDOW_SEC=180). Customers who leave PC on
#        but don't use the cloud UI no longer cost VPS pre-warm cycles.
#      • /api/bridge/me/local-status now has a 5 s in-memory cache per
#        user_id — cuts ~30× MongoDB reads while UI still feels live.
#      • /api/clicks/export switched from `to_list(1_000_000)` to
#        cursor iteration with a hard cap (EXPORT_CLICKS_MAX=100000);
#        response includes `total_available` + `truncated` so the UI
#        can prompt users to narrow the date range. Peak RAM per export
#        is now ≈ 50 MB instead of ≈ 500 MB. This was the biggest single
#        contributor to the "VPS stuck" reports.
#      • /api/clicks default-all path capped at 10 000 rows (was
#        100 000) — UI paginates at 50/page so this is invisible.
#
#   4) SECURITY / STABILITY
#      • Admin password reset now actually works. reset_password wrote
#        the new hash to `admin_config` but admin_login never read it —
#        login always compared plaintext against env ADMIN_PASSWORD, so
#        "Forgot password" was a silent no-op. Login now consults
#        admin_config first, falls back to env only if no override.
#      • Brute-force protection on /admin/login, /auth/login, and
#        /sub-users/login — 8 failed attempts per (email:IP) in a
#        15 min window → HTTP 429 + Retry-After header. Successful
#        login clears the counter. Env-tunable
#        (MAX_LOGIN_FAILURES, LOGIN_LOCKOUT_WINDOW_SEC).
#      • Admin login uses hmac.compare_digest against the env password
#        (constant-time) so timing side-channels are eliminated on
#        the fallback path.
#      • Removed noisy "password match: True/False" log line from
#        admin_login (info leak in supervisor logs).
#      • JWT_SECRET_KEY default `your-secret-key-change-in-production`
#        detected → a random 48-byte secret is generated for that run
#        and a big WARNING is logged. Prevents forged admin JWTs when
#        .env is misconfigured.
#      • ADMIN_PASSWORD == "admin123" default → WARNING logged.
#      • get_country_from_ip() bug fix — the returned `is_proxy` field
#        was mistakenly set to the `is_vpn` value, so real proxy IPs
#        were mislabelled in analytics. One-line fix.
#      • CORS: legacy allow_credentials=True + origins=["*"] was invalid
#        per spec (browsers refuse cookies to wildcard). Now: wildcard
#        implies credentials=False; concrete list implies credentials=
#        True. Set CORS_ORIGINS to your prod origins to enable creds.
#      • Sub-users now receive WebSocket live-click updates. The WS
#        handler used to only look in `users` collection, so sub-user
#        connections closed with 4002. Now honours the is_sub_user JWT
#        claim and joins the parent's channel.
#      • /api/system/install-update `is_admin` check moved BEFORE the
#        cloud-bridge relay — previously a non-admin sub-user on
#        krexion.com could trigger a container rebuild on their
#        parent's PC.
#      • Removed a dead POST /api/admin/releases stub that always
#        raised 500.
#
#   Verified live in preview:
#     • Admin login + password-reset override path both work.
#     • Brute-force lockout triggers at attempt 9 (HTTP 429).
#     • IPv4 first visit → 302, IPv6 first visit → 302.
#     • IPv4 duplicate → 403, IPv6 duplicate → 403.
#     • Clicks UI: IPv4 column shows only IPv4, IPv6 column shows only
#       IPv6, no row shows "-" for a real visitor.
#     • /api/clicks/export returns truncated + cap + total_available.
#
#   Migration notes:
#     • No DB schema change required. New `ipv6` field is written on
#       new clicks only — old rows continue to render whatever they
#       already have (frontend gracefully falls back).
#     • No customer-side change required. sync_client / bridge worker
#       protocol unchanged.
#     • VPS: no docker-compose change required — new env vars all have
#       sensible defaults. To further tune the pre-warmer, set
#       BRIDGE_PREWARM_MAX_PARALLEL and BRIDGE_PREWARM_UI_WINDOW_SEC
#       in backend/.env.
#
# Force redeploy trigger 1782418017
# v2.1.77 — 2026-07-01 — Native frontend path fix (v2.1.76 hotfix)
#   Customer report: opened http://127.0.0.1:8001/ after installing
#   v2.1.76 and saw `{"detail":"Not Found"}` instead of the Krexion
#   UI.
#
#   Root cause: In the native Inno Setup bundle, `server.py` lives at
#     {app}\bin\app\server.py
#   so `ROOT_DIR = Path(__file__).parent = {app}\bin\app`. The v2.1.76
#   frontend mount was searching:
#     ROOT_DIR.parent / "frontend"  =  {app}\bin\frontend  ← wrong
#   but the installer copies the React build to:
#     {app}\frontend  (see krexion-setup.iss line 106)
#   which is TWO parents up. The mount fell through to the "no build
#   found" branch → SPA catch-all never registered → GET / returned
#   404 with a JSON error body from FastAPI's default 404 handler.
#
#   Fix: search ROOT_DIR.parent.parent / "frontend" FIRST (correct
#   for native install), then fall back to ROOT_DIR.parent variants
#   for other layouts. Also elevated the "no build found" log from
#   info → warning so any future path mismatch is easier to spot in
#   `logs\backend-service.log`. Route table, guardrails, and
#   API-first-match behaviour unchanged.
#
#   Cloud VPS (KREXION_MODE=cloud) still skips this block entirely so
#   the v2.1.76 → v2.1.77 diff has ZERO effect on the cloud deploy.
#
#   Tests: same 7-path suite as v2.1.76, verified locally with
#   FRONTEND_BUILD_DIR override AND with the actual `.parent.parent`
#   layout by placing an index.html two directories above server.py.
#
# v2.1.76 — 2026-07-01 — Native install local frontend + bridge cache restored
#   1. Native / Electron desktop backends now serve the full Krexion
#      React UI locally on 127.0.0.1:8001 (the same port the backend
#      already listens on). Previously the local install had only two
#      services (KrexionDatabase, KrexionBackend) but no frontend
#      service — the customer had to open krexion.com in a browser,
#      which forced every UI action through the cloud bridge with a
#      30-60 s cache lag (v2.1.73 cache freshness = 30 s).
#      Result on 32 GB RAM customer PC: Visual Recorder screenshot
#      polling (designed at 700 ms) actually landed every 30-60 s so
#      the customer saw a frozen preview and reported "link proper
#      open ni hota". Live Visual Grid tiles for RUT jobs froze on
#      stale data → "RUT slow, live activity stuck". Browser Profile
#      launches sat in the browser_launch_queue for 30 s+ before the
#      cloud bridge propagated the click → "browser profile launch
#      hi ni hoti".
#      Fix: server.py now mounts the frontend build (already copied
#      by the Inno Setup installer into {app}\frontend\, and already
#      baked with REACT_APP_BACKEND_URL=http://127.0.0.1:8001 by the
#      Windows build workflow) at "/", with a SPA catch-all so React
#      Router client-side routes work. Guarded by KREXION_MODE != cloud
#      so the VPS deploy continues to serve the frontend via its
#      dedicated container (no cloud impact). /api/* + /r/{code}
#      routes are registered BEFORE the catch-all so they always
#      match first — no interception.
#      Effect on the 3 customer-reported issues:
#        • Visual Recorder screenshot poll: 30-60 s → 700 ms (60× faster)
#        • Live Visual Grid tile updates: 3-30 s → real-time (300× faster)
#        • Browser Profile launch: 30 s → 2 s (queue polling only)
#        • RUT job start latency: 30-60 s → < 500 ms (60× faster)
#      Public short-link URLs (krexion.com/r/{code}) UNAFFECTED —
#      POST /api/links is in cloud_proxy_module _CLOUD_PATH_EXACT so
#      link creation always forwards to VPS. Customer link URLs stay
#      the same forever; VPS keeps serving redirects 24/7 even when
#      the customer's PC is off.
#      Tray launcher (`desktop/krexion_tray_launcher.bat`) now opens
#      http://127.0.0.1:8001/ in the customer's default browser after
#      spawning the PyWebView tray, giving one-click access to the
#      full local UI.
#
#   2. Bridge response cache pre-warmer restored (was v2.1.75, reverted
#      in b024caa when deploy blocked on mongo unhealthy, unrelated).
#      Cloud UI users (mobile access from anywhere) now get 90 s fresh
#      cache + 25 s background pre-warm loop → RUT jobs page loads
#      in < 2 s instead of the previous 60 s cold-bridge wait on
#      first open after inactivity. VPS mongo gets a TTL index on
#      bridge_response_cache.stale_until so old rows auto-purge.
#
#   3. Structure guarantees for the customer:
#        • Existing customers' shortcuts still work (backwards compat)
#        • Public link URLs unchanged forever
#        • Local mongo data preserved (path + port unchanged)
#        • KREXION_MODE=cloud VPS unaffected — cloud UI still served
#          by its dedicated frontend container
#        • KREXION_MODE=cloud path skips the mount entirely (guarded)
#
# v2.1.69 — 2026-06-28 — Native install fixes (memory + browser profile launch)
#   1. RUT memory throttle no longer falsely fires on Windows native
#      installs. `_detect_container_mem_limit_mb()` previously fell back
#      to a hardcoded 6 GB cap when cgroup files weren't present (which
#      is ALWAYS the case on Windows), causing the customer's 32 GB
#      machine to throttle at backend RSS > 4.9 GB — well within
#      normal Playwright + Chromium territory. As a result every RUT
#      job got stuck on "running · 0/N visits" even though the host
#      had tons of free RAM.
#      Fix: third detection step uses psutil.virtual_memory() when
#      cgroup is absent, deriving cap = (total_ram - 4 GB) × 0.85
#      clamped to [1 GB, 32 GB]. 8 GB customer machines correctly get
#      ~3.5 GB cap (throttles before OOM); 32 GB machines get ~24 GB
#      cap (no false throttle). Cloud VPS path (cgroup) unchanged.
#      Tests: 9/9 in tests/test_native_mem_limit_fallback.py.
#
#   2. Browser Profile launch on NSSM-installed Windows build no longer
#      stuck on "launching..." forever. Root cause: Windows Services
#      run in Session 0 and cannot display GUI windows on the user's
#      desktop. Customer clicked Launch → backend (Session 0) spawned
#      Chromium → process existed but window invisible. Electron build
#      never hit this because its backend is a child of the Electron
#      user-session main process.
#      Fix: NSSM-build backends (KREXION_BUILD_TYPE=binary) now write
#      pending launches into a new `browser_launch_queue` Mongo
#      collection. The tray-app dashboard (already running in user
#      session via HKCU Run autostart) polls every 2s and spawns
#      Chromium IN ITS OWN PROCESS — Session 1, full desktop access,
#      Chromium window appears normally. Stop requests propagate the
#      same way via `stop_requested` flag on the queue record.
#      Tests: 11/11 in tests/test_browser_profile_user_session_handoff.py.
#      Backward compat: Electron + cloud VPS + sync_client bridge all
#      unchanged (defer only fires when both `sys.platform == 'win32'`
#      AND `KREXION_BUILD_TYPE == 'binary'`).
#
# v2.1.68 — 2026-06-27 — RUT reliability + Live Visual Grid bugfix
#   1. Live Visual Grid no longer hides actively-running visits.
#      `isVisitActive()` was checking per-step `latest_event.status === 'ok'`
#      which fired on EVERY successful automation step — so the grid stayed
#      empty even when 118-step flows were running in the background.
#      Customers saw "Waiting for first visit to start…" forever.
#      Fix: use VISIT-level `v.status` only as the done-signal; the
#      `stage:'done'` short-circuit catches the race where push_live_step
#      emits the final event before the dispatcher flips v.status.
#   2. Header tile counter (✓ / ✗ / ⏵) now uses the same correct signals
#      instead of per-step status. Counters and grid agree again.
#   3. Stuck-watchdog default raised 240s → 600s. Survey-heavy multi-deal
#      offers (like "target 750 v10") routinely have 5-8 min idle pauses
#      on the post-survey wait page — 240s was too aggressive and was
#      mass-aborting otherwise-healthy visits. UI input range also
#      widened to 30-1800s to match the backend cap.
#   4. Mid-flow chrome-error://chromewebdata/ is now tagged as a transient
#      tunnel failure (proxy dropped during form submit). The
#      `target_mode=conversions` dispatcher excludes these from the
#      max_attempts budget so a single proxy hiccup mid-flow no longer
#      burns one of the customer's conversion attempts.

